node_modules/

# Salidas de compilación (se regeneran: npm run dist / pwa / icon / capturas / gradlew)
dist/
paquetes/
pwa-dist/
tienda/
microsoft-store/build/appx/
microsoft-store/capturas/
microsoft-store/logos-ficha/
*.apk
*.aab

# Claves de firma. NUNCA deben entrar al repo: es publico.
# El .jks y su contrasena viven fuera del proyecto, en ../Catculator-claves/
*.jks
*.keystore
keystore.properties

# Lanzador local de escritorio (rutas de esta maquina; no va al repo publico)
Abrir Catculator.lnk
catculator-lanzador.vbs

# Basura del sistema
Thumbs.db
desktop.ini
.DS_Store
