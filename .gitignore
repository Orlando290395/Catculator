node_modules/

# Salidas de compilación (se regeneran: npm run dist / npm run pwa / gradlew)
dist/
pwa-dist/
*.apk
*.aab

# Claves de firma. NUNCA deben entrar al repo: es publico.
# El .jks y su contrasena viven fuera del proyecto, en ../Catculator-claves/
*.jks
*.keystore
keystore.properties

# Basura del sistema
Thumbs.db
desktop.ini
.DS_Store
